# README v0.2 Surface Refresh - Tech Spec

Product spec: `specs/GH220/product.md`
Tasks: `specs/GH220/tasks.md`
GitHub issue: `#220`

## Context

- `README.md` is the user-facing source for install and usage examples.
- Current CLI help can be generated by `cargo run -- --help` and subcommand
  help.
- `Cargo.toml` version is `0.2.0`.

## Proposed Changes

1. Audit README Current Status and Usage against CLI help.
2. Add or update examples for:
   - no-arg default flow
   - TUI default and `?` explain
   - `free <size>`
   - staleness and `stale_after_days`
   - recovery summaries
   - `completions`
   - `man`
   - cargo install, cargo-binstall, and Homebrew
3. Remove stale future-release phrasing.
4. Keep product boundaries and safety-model wording intact.

## Safety And Compatibility

- Docs-only. No runtime behavior changes.
- Do not overpromise cleanup scope or weaken safety language.
- Keep commands copy-pasteable.

## Validation

Focused:

```sh
rg -n 'After public release|free <size>|completions|man|stale_after_days|cargo binstall|brew install' README.md
cargo run -- --help
cargo run -- scan --help
cargo run -- free --help
```

Docs-only minimum:

```sh
cargo fmt -- --check
```
